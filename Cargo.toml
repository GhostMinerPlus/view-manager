[package]
name = "view-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
async-recursion = "1.1"
json = "0.12"

edge_lib = { git = "https://github.com/GhostMinerPlus/edge_lib.git" }

[dev-dependencies]
tokio = { version = "1.40.0", features = ["full"] }
